<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xmlns:jee="http://www.springframework.org/schema/jee" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.2.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.2.xsd  http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache-3.2.xsd">


    <context:component-scan base-package="tds.student.performance"/>
    <context:property-placeholder location="classpath*:performance-student.properties"/>

    <!-- Employee DB data source. -->
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
          destroy-method="close">
        <property name="driverClass" value="${performance.datasource.driverClassName}" />
        <property name="jdbcUrl" value="${performance.datasource.jdbcUrl}" />
        <property name="user" value="${performance.datasource.username}" />
        <property name="password" value="${performance.datasource.password}" />
        <property name="maxPoolSize" value="${performance.datasource.maxPoolSize}" />
        <property name="minPoolSize" value="${performance.datasource.minPoolSize}" />
        <property name="maxStatements" value="${performance.datasource.maxStatements}" />
        <property name="checkoutTimeout" value="${performance.datasource.checkoutTimeout}" />
        <property name="testConnectionOnCheckout" value="${performance.datasource.testConnectionCheckout}" />
        <property name="dataSourceName" value="Perf-DataSource" />
    </bean>

    <!-- Spring Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <!-- Annotations to manage transaction -->
    <tx:annotation-driven proxy-target-class="true" transaction-manager="transactionManager" />

    <!-- To disable all caching, comment out the cache:annotation-driven line below AND change the enabled property to false for the cacher bean -->
    <cache:annotation-driven key-generator="cacheKeyGenerator" />

    <bean id="cachingService" class="tds.student.performance.caching.impl.CachingServiceImpl" >
        <property name="enabled" value="true" />
    </bean>

    <bean id="cacheKeyGenerator" class="tds.student.performance.caching.CacheKeyGenerator" />

    <bean id="cacheManager" class="org.springframework.cache.ehcache.EhCacheCacheManager" p:cacheManager-ref="ehcache" />

    <bean id="ehcache" class="org.springframework.cache.ehcache.EhCacheManagerFactoryBean" p:configLocation="classpath:ehcache.xml" p:shared="true" />

    <bean id="itemBankService" class="tds.student.performance.services.impl.ItemBankServiceImpl" />
</beans>
